# Tested with conda env `donut_vml` on RTX 5090 (PyTorch 2.11.0+cu128, transformers 5.12.1)
# 모든 버전은 donut_vml 의 설치본에 하드 핀(==). torch/torchvision 은 cu128(Blackwell sm_120) 빌드라
# 아래 PyTorch 휠 인덱스에서 설치됨 (일반 PyPI 에는 +cu128 빌드가 없음).
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0+cu128
torchvision==0.26.0+cu128
transformers==5.12.1     # eval_strategy 파라미터명 변경 (4.45 이상 필요)
datasets==5.0.0
accelerate==1.14.0       # Seq2SeqTrainer 필수 의존성
Pillow==12.2.0
matplotlib==3.10.9
tqdm==4.68.3
numpy==2.2.6
sentencepiece==0.2.1     # DonutProcessor 토크나이저 의존성
tokenizers==0.22.2
pyyaml==6.0.3
timm==1.0.27             # Donut Swin 인코더 백본

# YOLO → Donut 파이프라인 검출/래스터화 단계 (0~2단계)
ultralytics==8.4.80      # YOLOv11 view/element 검출
opencv-python==4.13.0.92 # crop_utils OBB rectify(perspective warp)
pymupdf==1.27.2.3        # PDF 300 DPI 래스터화 (fitz)

# Optional — donut_vml 에는 미설치 (학습 곡선 모니터링 시 주석 해제: pip install wandb==0.28.0)
# wandb==0.28.0
