# Tested with torch_211_env (PyTorch 2.11.0+cu130, transformers 5.3.0)

torch>=2.0.0
transformers>=4.45.0    # eval_strategy 파라미터명 변경 (4.45 이상 필요)
datasets>=2.16.0
accelerate>=0.26.0      # Seq2SeqTrainer 필수 의존성
Pillow>=9.0
matplotlib>=3.7
tqdm>=4.65
numpy>=1.24
sentencepiece>=0.1.99   # DonutProcessor 토크나이저 의존성
tokenizers>=0.15.0
pyyaml>=6.0

# Optional
wandb
