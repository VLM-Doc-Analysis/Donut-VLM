# Model checkpoints (large binary files)
# checkpoints/
# checkpoints_drawings/
# checkpoints_elements/

# LFS 비용 절감: 중간 체크포인트(checkpoint-NNN/)·실험성 런은 추적 제외, final/ 만 커밋.
#   (추론엔 final/ 만 필요. optimizer.pt 등 학습재개 상태가 용량 대부분이라 비커밋)
checkpoints*/checkpoint-*/
checkpoints/test_run/
checkpoints/test_211/

# YOLO training outputs
runs/
# detection/**/runs/
# *.pt

# SROIE 데이터 (노트북 git clone 셀로 재생성 가능 — 커밋 안 함)
SROIE_donut/data/

# hftuner: 외부 donut 라이브러리 클론. 이미지 링크 로컬화 후 GitHub 에도 올리도록 추적 전환.
#   (내부 .git 은 제거해 일반 파일로 편입. __pycache__ 는 아래 규칙으로 제외)
# donut_finetune_classification/hftuner/
# 분류 학습 산출물 (재생성 가능)
donut_finetune_classification/donut-classification-turbo/
# 파싱 노트북 클론 라이브러리 + 학습 산출물 (재생성 가능)
# donut_finetune_parsing/hftuner/
donut_finetune_parsing/donut-base-finetuned-sroie/
donut_finetune_parsing/donut-base-finetuned-sroie-hires/

# 실험 부산물 (3/6클래스 비교 등 일회성 산출 — 재생성 가능, 커밋 안 함)
checkpoints_elements_3cls/
checkpoints_elements_all/
data/processed_elements_3cls/
data/processed_elements_all/
data/_pipeline_tmp/

# Dataset cache
# data/

# Detection datasets (CVAT/YOLO 이미지·라벨) 및 생성 산출물
# datasets/
# output/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
# donut_base_test.ipynb
donut_finetune_parsing/donut-base-finetuned-sroie-1280x1600/
